// TODO change to CONST UPPERCASE
// read as question/answer types
// classic js style transposed to ruby-style our api uses
export const qatypes = {
  numberInput: 'number_input',
  textInput: 'text_input',
  textarea: 'textarea',
  select: 'select',
  radio: 'radio',
  checkbox: 'checkbox',
  header: 'header'
};

export const optionTypes = ['select', 'radio', 'checkbox'];

// TODO change loading to loading for Answering
export const purposes = {
  building: 'building',
  editing: 'editing',
  loading: 'loading',
  loadForEdit: 'loadingForEditing',
  deleting: 'deleting'
};
